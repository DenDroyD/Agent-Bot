# Telegram бот с RAG на основе документации

1. Установите зависимости: `pip install -r requirements.txt`
2. Поместите HTML-файлы в папку `documents`.
3. Запустите индексацию: `python indexer.py`
4. Создайте файл `.env` с токенами.
5. Запустите бота: `python bot.py`